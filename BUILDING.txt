NOTE: This is now handled by the buildbot, you only need to change the source
      files and the buildbot will regenerate the site and publish it.
      
# Building the web site

* Edit source/versions.json to update version numbers, dates, or news items
* Edit source/templates/*.template.html for layout changes
* open a shell, go to the source/ dir
* Run: python3 generate.py
* Commit the changes to git :)
